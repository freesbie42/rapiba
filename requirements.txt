# Rapiba Requirements
# pip install -r requirements.txt

# Core
# (Python 3.6+ Standardbibliothek, keine zusätzlichen Anforderungen)

# Optional für Admin-Tool
tabulate>=0.8.0

# Development / Testing
pytest>=6.0
pytest-cov>=2.10.1
